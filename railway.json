{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema backend/prisma/schema.prisma && npm run build && cp -R web/dist backend/dist/web"
  },
  "deploy": {
    "startCommand": "npx prisma db push --schema backend/prisma/schema.prisma && WEB_DIST_DIR=backend/dist/web node backend/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "healthcheckPath": "/api/healthz",
    "healthcheckTimeout": 30
  }
}
